import 'package:flutter/material.dart';

class OrderWaitingPayment extends StatelessWidget {
  const OrderWaitingPayment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Esperando o pagamento'),
    );
  }
}
